# 設計判断について（DECISIONS）

このドキュメントは、READMEには書ききれない「なぜその技術・設計を選んだか」をまとめたものです。作った本人が、判断の理由と、そのトレードオフをどう認識しているかを共有する目的で書いています。

---

## 1. フレームワークを使わなかった理由

React・Vueなどのフレームワークを使う選択肢もありましたが、意図的にHTML・CSS・TypeScriptのみで実装しました。

このサイト以外の制作物（Site Check、Nedaka.）ではReactを使っており、フレームワークを使った実装力はそちらで証明できています。ポートフォリオサイト自体は逆に、「フレームワークに頼らずとも、DOM操作・アニメーション・状態管理を素の技術で組み立てられる」という基礎力を見せる場として位置づけました。技術選定を、単なる好みではなく「何を証明したいか」から逆算した例です。

## 2. 後からTypeScript化した理由

当初は素のJavaScriptで実装していましたが、後から型を付けてTypeScriptに書き直しました。ビルドツールを持ち込みたくなかったため、TypeScriptのソース（`src/main.ts`）をコンパイルし、その結果のJSファイルを直接HTMLに埋め込む、という構成にしています。ソースコードはリポジトリ内に残しているため、実際にTypeScriptで書いていることも確認できます。

型を付けることで、DOM要素の取得時に起きがちな「実際にはnullかもしれない要素を、null チェックなしで操作してしまう」ようなミスをコンパイル時点で検出できるようになりました。

## 3. スキルレーダーチャートをSVGで自作した理由

グラフ描画ライブラリを使えば数行で実装できますが、あえて座標計算から自分でSVGを組み立てました。6軸のスキル項目を、角度と半径から座標に変換するロジックを自分で書くことで、「ライブラリの使い方を知っている」以上の、幾何学的な実装力を見せられると考えました。

## 4. Canvas APIのミニ製図ツール（Playground）を置いた理由

プラント設計エンジニア時代にCADで図面を引いていた経験と、現在Web開発をしている経験を繋げる要素として、ドラッグで線を引けるCanvasツールを置きました。単なる自己紹介の文章だけでなく、「実際に手を動かして分かる」形でキャリアの連続性を表現することを狙っています。

## 5. イラストをAI生成で用意した理由

About・Profile・Thoughtsセクションの背景イラストと、Worksのサムネイル画像は、AI画像生成（ChatGPT）で作成しています。イラストレーターとしての実力を主張する場ではないため、外部ツールで効率的に用意する判断をしました。一方で、これを隠さずREADMEに明記しているのは、「AIツールを適切に使い分けられる」ことも、今のエンジニアに求められるスキルの一つだと考えているためです。

## 6. GitHub Pagesでホスティングしている理由

個人のポートフォリオサイトという用途では、サーバーサイドの処理が不要で、無料かつ`username.github.io`という覚えやすいURLで公開できるGitHub Pagesが最適だと判断しました。

---

## 想定される質問への回答

**Q. なぜReactで作らなかったのですか？**
Site CheckやNedaka.でReactを使った実装力はすでに証明できているため、このサイトでは逆に「フレームワークなしでどこまでできるか」を見せる目的で、あえてvanilla構成を選びました。技術選定の理由を説明できることを重視しています。

**Q. AI生成のイラストを使うことに抵抗はなかったですか？**
むしろ積極的に使うべきだと考えています。イラスト制作は自分の専門領域ではないので、その部分は適切なツールに任せ、自分の時間は設計やロジックの実装に使う、という判断です。使ったこと自体を隠さず開示している点も含めて、誠実さのアピールになると考えています。
